# Copy this file to backend/.env and fill in the values.

# SQLite database path (relative to backend/ directory)
# Default: sqlite:///data/entries.db  (usually no need to override)
# DATABASE_URL=sqlite:///data/entries.db
